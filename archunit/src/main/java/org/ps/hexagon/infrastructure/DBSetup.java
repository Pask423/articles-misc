package org.ps.hexagon.infrastructure;

import org.ps.hexagon.config.DBConfig;

public class DBSetup {

    private final DBConfig dbConfig;

    public DBSetup(DBConfig dbConfig) {
        this.dbConfig = dbConfig;
    }
}
